# ============================================================
# ROOT .gitignore — applies to entire monorepo
# Each sub-project (mobile/, backend/) has its own .gitignore
# for framework-specific ignores.
# ============================================================

# ------------------------------------------------------------
# OS Files
# ------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ------------------------------------------------------------
# IDE & Editor
# ------------------------------------------------------------
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# ------------------------------------------------------------
# Environment Variables & Secrets
# ------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.cert
*.p12
secrets/

# ------------------------------------------------------------
# Logs & Debug
# ------------------------------------------------------------
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ------------------------------------------------------------
# Dependency directories (root-level only)
# Sub-projects have their own
# ------------------------------------------------------------
node_modules/

# ------------------------------------------------------------
# Build & Cache (root-level tools)
# ------------------------------------------------------------
.cache/
.turbo/
.nx/cache/
dist/
build/
out/

# ------------------------------------------------------------
# Package manager locks (keep only one — avoid conflicts)
# Uncomment the one your team doesn't use:
# ------------------------------------------------------------
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# ------------------------------------------------------------
# Misc
# ------------------------------------------------------------
*.zip
*.tar.gz
*.tgz
.tmp/
tmp/
